<h1>Listing users</h1>

<table class="table table-bordered">
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user, class:'btn btn-danger' %></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>

